{
  "name": "wxt-safari-demo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "wxt build -b safari",
    "build:mv3": "wxt build -b safari --mv3",
    "test": "vitest run",
    "prepare-types": "wxt prepare"
  },
  "dependencies": {
    "wxt-module-safari-xcode": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^4.1.10",
    "wxt": "^0.21.3"
  }
}
